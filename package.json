{
  "name": "hera-preconsult-stage1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && npm run check:vercel && node --test tests/*.test.mjs && npm run check:ui",
    "demo": "npm run build && node demo.mjs",
    "check:ui": "node --check public/app.js",
    "check:vercel": "tsc -p tsconfig.vercel.json"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
